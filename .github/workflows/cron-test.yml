name: Cron Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  cron-test:
    runs-on: ubuntu-latest
    steps:
      - name: Show trigger time
        run: |
          echo "Cron triggered successfully"
          date -u
